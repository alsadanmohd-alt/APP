# تحديث الدخول بالجوال والتأمين

## تفعيل الجوال

في Supabase → Authentication → Providers فعّل Phone واضبط مزود SMS مثل Twilio أو المزود المدعوم المناسب لحسابك. فعّل إنشاء المستخدمين، واجعل طول الرمز 6 أرقام ليتطابق مع الواجهة. راجع حدود إرسال الرسائل وتكلفة المزود قبل استخدامه فعليًا. بيانات المزود تبقى في إعدادات Supabase ولا توضع في المتصفح.

التسجيل والدخول يستخدمان رقم الجوال ورمز SMS، دون البريد أو كلمة المرور. تدعم الواجهة الرقم السعودي بصيغة 05xxxxxxxx والأرقام الدولية بصيغة +رمز الدولة، وإعادة الإرسال وتعديل الرقم. لا يُمنح الدخول إلا بعد التحقق من الرمز في Supabase. لإيقاف إنشاء حسابات بالبريد عبر API أيضًا عطّل مزود Email في Supabase.

الحسابات القديمة ذات البريد لا تُدمج تلقائيًا مع حسابات الجوال. قبل تعطيل Email في قاعدة مستخدمة، اربط أرقام الجوال الموثقة بالمستخدمين القدامى عبر مسار إداري موثوق يحافظ على UUID؛ وإلا سينشأ حساب جديد بأغراض وحجوزات منفصلة. لا تغيّر مالكية البيانات اعتمادًا على رقم يدخله المستخدم وحده.

## قاعدة البيانات

- قاعدة جديدة: نفّذ `supabase/schema.sql` فقط.
- قاعدة سبق إعدادها بالنسخة الأولى: نفّذ `supabase/migrations/002_phone_deposit.sql` مرة واحدة فقط، قبل تشغيل الواجهة المحدثة. لا تُعد تنفيذ المخطط الكامل.
- الترحيل يحافظ على الأغراض والحجوزات ويعطي السجلات السابقة مبلغ تأمين صفرًا. لا يضيف تأمينًا بأثر رجعي.

## معنى التأمين في هذه النسخة

خيار «بدون تأمين» يحفظ صفرًا، وخيار «بتأمين مسترد» يتطلب مبلغًا موجبًا بحد أقصى 100000 ر.س. المقصود مبلغ ضمان مسترد، وليس وثيقة تأمين من شركة. يظهر في الإعلان والتفاصيل وملخص الحجز منفصلًا عن أجرة الإيجار. تستخرج قاعدة البيانات المبلغ من الغرض عند إنشاء الحجز وتحفظ نسخة ثابتة منه؛ لا يستطيع المستأجر تغييره مباشرة.

لا يوجد تحصيل أو رد آلي للأموال. يدفع الطرفان ويعيدان مبلغ الضمان عند الاستلام والإرجاع حسب الاتفاق.

## اختبار التحديث

نفّذ `pnpm test` و`pnpm build`. بعد ربط Supabase ومزود الرسائل، جرّب رقمين مختلفين: إرسال الرمز، رفض الرمز الخاطئ، التحقق الصحيح، إعادة الإرسال وتغيير الرقم. ثم انشر غرضًا بدون تأمين وآخر بتأمين واحجزهما، وتحقق من أجرة الإيجار ومبلغ التأمين والإجمالي من الحسابين. إرسال SMS الفعلي وترحيل قاعدة البيانات لم يُنفّذا في بيئة التسليم لغياب الخدمة ومفاتيحها.

[دليل Supabase للدخول بالجوال](https://supabase.com/docs/guides/auth/phone-login)
