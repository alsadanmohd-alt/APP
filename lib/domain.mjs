export function distanceKm(a,b){const r=x=>x*Math.PI/180;const h=Math.sin(r(b.lat-a.lat)/2)**2+Math.cos(r(a.lat))*Math.cos(r(b.lat))*Math.sin(r(b.lng-a.lng)/2)**2;return 6371*2*Math.atan2(Math.sqrt(h),Math.sqrt(1-h))}
export function approximate(n){return Math.round(n*100)/100}
export function normalizePhone(value){let s=value.trim().replace(/[٠-٩]/g,c=>String('٠١٢٣٤٥٦٧٨٩'.indexOf(c))).replace(/[\s()-]/g,'');if(/^05\d{8}$/.test(s))s='+966'+s.slice(1);if(s.startsWith('00'))s='+'+s.slice(2);if(!/^\+[1-9]\d{7,14}$/.test(s))throw Error('أدخل رقم جوال صحيحًا مع رمز الدولة، مثل +9665xxxxxxxx');return s}
